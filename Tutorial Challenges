#include<stdio.h>
int main()
{
    int v,n;
    scanf("%d%d",&v,&n);
    int a[n];
    for(int i=0; i<n; i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=0; i<n; i++)
    {
        if(a[i]==v)
        {
            printf("%d",i);
        }
    }
}
